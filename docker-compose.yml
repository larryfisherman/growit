services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: growit
      POSTGRES_USER: growit
      POSTGRES_PASSWORD: localdev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
